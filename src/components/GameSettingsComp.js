import React from 'react'
import PropTypes from 'prop-types'

class GameSettingsComp extends React.Component {
  constructor(props) {
    super(props);
  }
  render() {
    return (
      <section id="game-settings-area">Game Settings Comp</section>
    )
  }
}

export default GameSettingsComp
